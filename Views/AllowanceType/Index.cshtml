@model IEnumerable<MVCProjectImplementationOfMasterDetails.Models.AllowanceType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AllowanceCategory.AllowanceCategoryName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AllowanceName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AllowanceCategory.AllowanceCategoryName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AllowanceName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AllowanceTypeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.AllowanceTypeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AllowanceTypeId })
        </td>
    </tr>
}

</table>
